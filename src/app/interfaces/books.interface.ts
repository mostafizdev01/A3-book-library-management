// import { Types } from "mongoose";

export interface IBook {
    title: string
    // content: string,
    // category: string,
    // pinned: boolean,
    // tags: string,
    // userId: Types.ObjectId
}